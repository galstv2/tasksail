You are running a confinement retry as a fresh independent Dalton process.
You do not have memory of the previous launch.

First repair or remove every out-of-bound change listed in this prompt. Then use
the Original Assignment Context below to compare the current codebase state
against the assigned implementation spec and slices. Continue from the current
state until the original acceptance criteria and validation commands pass.

Authoritative reference: `COPILOT_WRITABLE_ROOTS_JSON` defines what you may
write; `COPILOT_READONLY_CONTEXT_ROOTS_JSON` defines what you may read. Both are
JSON arrays of `{ path, kind, reason }` objects.
