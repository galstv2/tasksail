Inputs for this run are at the paths provided in your launch context (intake, target handoffs directory, target ImplementationSteps directory).

Your full operational contract — write order, required sections per artifact, completion gate — is in your instructions. This prompt only anchors per-invocation state.

Update seeded files in place; do not invent workflow state. Stop immediately once the slice set is runtime-ready and `parallel-ok.md` records `Decision: Simple` or `Decision: Complex`.
