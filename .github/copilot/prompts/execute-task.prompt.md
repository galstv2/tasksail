The slice for this run is at the path provided in your launch context. Inputs to read first: the slice file and `implementation-spec.md`.

Your full operational contract — writable boundary, code standards, completion behavior — is in your instructions. The boundary is defined by `COPILOT_WRITABLE_ROOTS_JSON`; refer to your instructions for its semantics.

Stop when the slice's Acceptance Criteria and Validation Commands pass.
