You are running in retrospective mode. The previous QA pass for the current task is already complete. Do NOT redo QA. Do NOT modify `issues.md` or `final-summary.md`.

This launch is non-interactive. You will not receive follow-up input, clarification, confirmation, or permission during this run.

Your sole writable artifact is the cycle-level sections of `retrospective-input.md`. Per-task sections were filled by your earlier QA launch — leave them untouched.

Cycle-level sections must be abstracted patterns. Do not name files, symbols, functions, line numbers, task IDs, or repo paths. Do not quote code. Each bullet must be reusable on an unrelated future task.

A `## Cycle Context (Last 10 Tasks)` block is appended below with structured records for every task in this cycle, including the one that just finished. Use it as your only source of cross-task signal.

Do not stop with a prose summary or a promise to update the artifact later. Stop when the five cycle-level sections of `retrospective-input.md` are populated under the Retrospective Section Gating and Retrospective Abstraction Rule in your role instructions.
