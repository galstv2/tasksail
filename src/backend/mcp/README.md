# Backend Source Root

Runtime backend code lives under this subtree.

Current migration status:

- `repo_context_mcp/` is the backend package home for the repo-context MCP runtime.
- Root-level `scripts/` remain stable operator entrypoints during migration.
- Docker packaging and legacy runtime paths may continue to exist temporarily as compatibility seams.
