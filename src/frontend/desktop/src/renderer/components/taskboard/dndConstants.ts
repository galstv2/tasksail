/** Custom MIME type identifying the source column of a dragged task card. */
export const DND_MIME_SOURCE_COLUMN = 'application/x-source-column';

/** Custom MIME type set only on error-column cards, enabling the Open column to filter dragOver. */
export const DND_MIME_MOVABLE_TO_OPEN = 'application/x-movable-to-open';
