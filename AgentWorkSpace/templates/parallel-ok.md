# Parallel OK

Use this file to signal task complexity to the platform.

## Task Metadata

- Task ID:
- Task Title:
- Initialized At (UTC):
- Active Branch:
- Intake Source:

## Decision
<!-- (1 word) — write "Simple" or "Complex". Writing "Complex" activates validation of Independent Slices and Constraints below. -->

## Independent Slices
<!-- (1+ bullet items required when Decision is "Complex"; prose listing fails validation — slice IDs that can run concurrently. Each ID must match a real ImplementationSteps/<sliceId>.md file or validation fails.) -->

## Constraints
<!-- (1-3 bullets or write "None"; required when Decision is "Complex" — resource limits, ordering dependencies. Empty triggers a warning.) -->

## Coordination Notes
<!-- (1-2 sentences) — merge strategy, shared state concerns; "None" if not applicable -->
